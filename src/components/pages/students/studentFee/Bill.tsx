import { BillProps } from "@/types";
import styles from './bill.module.css';

export default function Bill({bill_items, student_name, roll_no, date,student_class}:BillProps){
    return <div className={styles.billContainer}>

    </div>

}